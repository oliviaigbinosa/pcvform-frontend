[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Set VITE_API_BASE_URL to your Render backend URL in the Netlify dashboard.
# Example: VITE_API_BASE_URL=https://pcv-backend-xxxxx.onrender.com

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
